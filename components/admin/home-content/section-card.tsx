'use client';
import {ChevronDown,Pencil} from 'lucide-react';import {useState} from 'react';
export function SectionCard({title,description,enabled,dirty,children}:{title:string;description:string;enabled?:boolean;dirty?:boolean;children:React.ReactNode}){const[open,setOpen]=useState(false);return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><button type="button" onClick={()=>setOpen(!open)} aria-expanded={open} className="flex w-full items-center gap-4 p-5 text-left"><span className={`h-2.5 w-2.5 rounded-full ${enabled===false?'bg-slate-300':'bg-green-500'}`}/><span className="min-w-0 flex-1"><span className="flex items-center gap-2 font-semibold text-slate-950">{title}{dirty&&<span className="rounded-full bg-amber-50 px-2 py-0.5 text-[10px] text-amber-700">Sin guardar</span>}</span><span className="mt-1 block truncate text-sm text-slate-500">{description}</span></span><span className="hidden rounded-lg border px-3 py-2 text-xs font-semibold sm:inline-flex"><Pencil className="mr-1 h-3.5 w-3.5"/>Editar</span><ChevronDown className={`h-5 w-5 transition ${open?'rotate-180':''}`}/></button>{open&&<div className="border-t border-slate-100 p-5">{children}</div>}</section>}
